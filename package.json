{
  "name": "cih-sdg3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm run build",
    "dev": "cd Frontend && npm run dev",
    "vercel-build": "cd Frontend && npm run build"
  }
}
